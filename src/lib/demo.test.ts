import{createClient}from"@libsql/client";import{describe,expect,it}from"vitest";import{createOperation,listOperations,loadDemoOperation,resetDemoOperations}from"./operations";
describe("isolamento da demonstração",()=>{it("carrega idempotente e remove somente demo",async()=>{const db=createClient({url:":memory:"});const org="org-demo";await createOperation({title:"Produção sintética de teste",debtor:"",tribunal:"",process:"",owner:"",source:"teste",stage:"Entrada",nominal:0,notes:"",tasks:[],checks:[],proposals:[],isDemo:false},db,org,"u");const a=await loadDemoOperation(db,org,"u"),b=await loadDemoOperation(db,org,"u");expect(a.id).toBe(b.id);expect((await resetDemoOperations(db,org)).removed).toBe(1);const left=await listOperations(db,org);expect(left).toHaveLength(1);expect(left[0].isDemo).toBe(false);db.close()})});
